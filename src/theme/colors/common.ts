export type Black = '#000';
export type White = '#fff';
export type Common = {
  black: Black;
  white: White;
};

const common: Common = {
  black: '#000',
  white: '#fff',
};

export default common;
